apiVersion: v2
name: di-agent-system
description: >
  Helm chart for the di-agent PoC power-system simulation stack (Kafka,
  genset, switchboard, propulsion, battery, auxload, InfluxDB,
  telemetry-writer, Grafana, playground). Does not include the di-agent
  component itself, which is deployed one instance per cluster node via
  poc2/scripts/04-agent.sh.
type: application
version: 0.1.0
appVersion: "1.0.0"
